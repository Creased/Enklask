[project]
name = "enklask"
version = "0.1.0"
description = "Enklask — self-hosted multi-marketplace second-hand tracker (eBay, Vinted, Leboncoin)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
